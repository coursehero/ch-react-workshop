// Base URL for the server
export const BASE_URL = 'http://127.0.0.1:5005'

// The default doc rendered when there isn't a matching course in the route
export const DEFAULT_DOC = 'BIO-101'

// Error message when the network request fails
export const ERROR_PAGE_MESSAGE = 'Something went wrong...'

// Header text shown in the `RelatedDocs` component
export const RELATED_DOCS_VIEWED = 'Students who viewed this also studied'
